# Hairline divider under the row. Card chrome is stripped theme-wide by
# dos. !include'd as `card_mod:` across all views.
style: 'ha-card { border-bottom: 0.6px solid var(--dos-line) !important; }

  '
